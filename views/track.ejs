<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ติดตามสถานะ | ST STUDIO</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

<style>
body{
  background:#f8f9fa;
  font-family:'Segoe UI',sans-serif;
}
.header{
  background:#0d3b66;
  color:white;
  padding:20px 0;
}
.card{
  border-radius:20px;
}
.progress{
  height:25px;
}
.progress-bar{
  background:#0d3b66;
}
</style>
</head>

<body>

<div class="header text-center">
  <img src="/logo.png" width="90" class="mb-2">
  <h5>ST STUDIO</h5>
  <small>Sharpen Shop Pro</small>
</div>

<div class="container mt-4 mb-5">

  <div class="card shadow p-4 text-center">

    <h4 class="text-primary mb-3">สถานะงานลับคม</h4>

    <h5><%= job.customerName %></h5>
    <p class="text-muted">
      <%= job.itemType %> จำนวน <%= job.quantity %>
    </p>

    <%
    let percent =
      job.status === "รับงานแล้ว" ? 25 :
      job.status === "กำลังลับคม" ? 60 :
      job.status === "เสร็จแล้ว" ? 90 :
      100;
    %>

    <div class="progress mb-3">
      <div class="progress-bar" style="width:<%= percent %>%">
        <%= job.status %>
      </div>
    </div>

    <% if(job.imageUrl){ %>
      <div class="mt-4">
        <h6 class="text-success">ภาพงานเสร็จ</h6>
        <img src="<%= job.imageUrl %>" 
             class="img-fluid rounded shadow mt-2">
      </div>
    <% } else { %>
      <p class="text-muted mt-3">
        ยังไม่มีภาพอัปเดต
      </p>
    <% } %>

  </div>

</div>

</body>
</html>
